
********** GIT CODES *********

// git log (To See Details like
Commit ID (a unique hash)
Author (your name and email)
Date and time
Commit message)
// git checkout <ID>(To rollback to previous version of the file)
// git add <file name> (To Add a specific file in staging area)
// git add <file 1> <file 2> <file 3> (To add multiple files at once)
// git add . (To add all changes in files ) 
// git config --global user.name "NAME" (To Add User Name in Git Commit)
// git config --global user.email "EMAIL" (To Add User Email in Git Commit)
// git commit -am "Message" (TO skip git add if already modified and update in commit messages with changes)
// git show <commitid> (To show the commited Message)
// git restore <file-name> (To restore a file to the last commited version)



********** GIT REPOSITORY initialization *********

// git init (for git hub initialization)
// git add file name (specify file to be added)
// git add . (for all files)
// git commit -m "Desc name" (To commit Name)
// git branch -M main (To selct branch)
// git remote add origin <URL> (URL of the reposiory)
// git remote set-url origin (To refesh a remote repository link)
// git push -u origin main (to push the file in repository)
// git push -u origin <branch-name> (to push the changes into specific git branch)
// git checkout <branch-name> (To switch to a specifc branch)
// git checkout -b <branch-name> (To create and switch to a specifc branch)
// git pull (To pull a repository changes in local machine)
// git pull origin <branch-name> (To Bring updates from github branch) 
// git fetch origin (Download all the new commits from remote but do not merge)
// git log origin/main (To view fetched changes)
// git merge origin/main (To merge the changes in local machine manually)


********** GIT BRANCHES *********

// git branch (To View all Available branches)
// git branch <branch-name> (To create a New git branch)
// git branch -d <branch-name> (To delete a git branch when its merged)
// git branch -D <branch-name> (To force delete a git branch when its unmerged)
// git switch <branch-name> (To switch working on branch)
// git switch -c <branch-name> (To Create and switch on a branch at once)
// git switch master (To Switch to main branch)
// git branch -v (To View all branches with thier latest commits)
// git diff (To View difference in working directory vs latest commit)
// git diff --staged (To View difference staged not yet commited)
// git diff <branch1> <branch2> (To View difference in two branches)
// git diff <commit1> <commit2> (To View difference in two commit)



********** GIT BRANCHES MERGER *********

// git switch master (To Switch to main branch)
// git merge <branch-name> (Merges all changes from mentioned branch into master branch)


********** GIT RESTORE *********

// git restore <file-name> (To restore the file to the last commited version)
// git restore --staged <file-name> (Unstaged a file ffrom git)
// git reset --soft HEAD~1 (To undo the commit but keep the changes)
// git reset --hard HEAD~1 (To Undo the Commit and Delete the last changes)


********** GITHUB SYNC *********

// git clone <repository-url> (To clone a github repository)
// git stash (Temporarely save satged changes to a repository - Stash let you change a branch with saving current staged changes temporarly)
// git stash push -m "message" (To stash changes with a message)
// git stash show (#Shows a short summary of the latest stash changes)
// git stash show -p (Shows the full, detailed code changes of the latest stash)
// git stash apply (Apply changes from latest stash you saved)
// git stash apply stash@{n} ( n = number of stash saved to apply changes)
// git stash pop (Apply and Delete latest stash)
// git stash drop stash@{n} (Apply and Delete a Specific stash)
// git stash clear (Delete all stash)
// git rebase master (To clear all commit)
// git rebase --continue (To clear conflict during rebase manually)
// git rebase --abort (To undo a ongoing rebase)
// git log --oneline --graph --all (To see history)
// git cherry-pick <commit-hash> (To Apply specific commit from a branch)
// git cherry-pick --continue (To Fix errors during cherry pick manually)
// git cherry-pick --abort (To abort the cherry pick process)